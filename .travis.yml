sudo: false
language: python
python:
    - "3.4"
    - "3.5"
    - "nightly"
addons:
  apt:
    packages:
    - libatlas-dev
    - libatlas-base-dev
    - liblapack-dev
    - gfortran
    - libgmp-dev
    - libmpfr-dev
before_install:
    - export TESTMODE=fast
install:
    - travis_retry pip install numpy scipy matplotlib sqlalchemy pandas networkx sympy
    - travis_retry pip install seaborn jupyter notebook ipywidgets ipyparallel sphinx nose
    - travis_retry pip install traitlets gmpy2 exa codecov
script:
    - nosetests --with-doctest --with-coverage
after_success:
    - codecov
